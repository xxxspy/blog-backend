
<%
    var tags = [];
    var related_posts = [];
     var default_tag = null;
    if(page.tags && page.tags.length) {
        
        page.tags.forEach(function(tag){
            if(default_tag == null){
                 default_tag = tag;
            }
            tags.push('<a href="' + config.root + tag.path + '">' + tag.name + '</a>');
        });
    }
    if(default_tag != null){
        default_tag.posts.forEach(function(post) { 
            related_posts.push({
                title: post.title,
                url: (post.path)
            }) })
    }
     

%>
    
        
<div class="row">
    
    <div class="col-md-9 col-sm-12">
        <!--Title and Logo-->
        <section class="main">
            <%- partial('_partial/article-long', {item: page}) %>
        </section>
        <section>
            <div class="content" style="color: #d7ecff">
                <h3 id="赞助"><a href="#赞助" class="headerlink" title="赞助"></a>赞助</h3>
                <p>运营本站花费了很多时间和精力, 并且本站坚持不放广告, 如果你觉得本站对你有帮助, 请资助我一杯咖啡:</p>
                <p><img src="/img/收款二维码.png" class="img-thumbnail"></p>
            </div>
        </section>
        <!-- changyan comments -->
        <section class="main">
            <%- partial('_partial/changyan', {item: page}) %>

        </section>
    </div>

    <!-- rightside -->
    <div class="col-md-3 col-sm-12 rightside">
        <%- partial('_partial/post-right-side', {item: page}) %>
    </div>
</div>
