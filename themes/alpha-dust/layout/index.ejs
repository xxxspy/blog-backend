<div class="row">
    <%- partial('_partial/home-post-list', {tags: [
        site.tags.findOne({name:'python'}), 
        site.tags.findOne({name:'keras教程'}),
        site.tags.findOne({name:'视频教程'}),
        ]}) 
    %>
</div>
<div class="row">
    <%- partial('_partial/home-post-list', {tags: [
        site.tags.findOne({name:'tensorflow教程'}),
        site.tags.findOne({name:'机器学习'}),
        site.tags.findOne({name:'游戏'}),
        ]}) 
    %>
</div>
<div class="row">
    <div class="col-sm-12">
        <!--Title and Logo-->
        <section class="main">
            <% page.posts.forEach(function(item, index) { %>
                <%- partial('_partial/article-short', {item: item, index: index}) %>
                    <% }); %>
        </section>
    </div>
</div>

<%- partial('_partial/pagination') %>